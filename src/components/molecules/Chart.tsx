import { ParsingContext } from "@/constants";
import React, { useContext } from "react";

const Chart = () => {
  const { input } = useContext(ParsingContext);

  return <div>Chart</div>;
};

export default Chart;
